dist: xenial
language: python

python:
  - "3.4"
  - "3.5"
  - "3.6"

matrix:
  include:
    - python: "3.7"
      sudo: true
      env:
      - REQUESTS="requests" # latest

env:
 - REQUESTS="requests" # latest

install: pip install $REQUESTS

script: python setup.py test
